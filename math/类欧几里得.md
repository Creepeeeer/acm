求 $ \sum_{i=0}^{n}\left\lfloor \frac{ai + b}{c}\right\rfloor $

```c++
ll floorsum(ll a,ll b,ll c,ll n){
	if(a>=c||b>=c){
		return floorsum(a%c,b%c,c,n)+a/c*n*(n+1)/2+b/c*(n+1);
	}
	ll m=(a*n+b)/c;
	if(!m)return 0;
	return m*n-floorsum(c,c-b-1,a,m-1);
}
```

