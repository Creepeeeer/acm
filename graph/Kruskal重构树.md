重构树过程：

1. 图中每个点，都是Kruskal重构树的底层叶节点，初始时认为互不连通
2. 先考虑边权小的边，后考虑边权大的边，添加连通性的边就选择，不添加联通性的边就舍弃
3. 一条边一旦被选择，就变成kruskal重构树的非叶节点，并且作为头节点，连接两个联通的部分
4. 考虑完所有的边，过程停止，原始图中有几个联通区域，就有几棵Kruskal重构树
5. 每个Kruskal重构树，都进行树上dfs，建立倍增表等信息

Kruskal越往上，代表边权越大，获得的连通性也越大



