**可行顶标**：给每个节点 𝑖![i](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7) 分配一个权值 𝑙(𝑖)![l(i)](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)，对于所有边 (𝑢,𝑣)![(u,v)](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7) 满足 𝑤(𝑢,𝑣) ≤𝑙(𝑢) +𝑙(𝑣)![w(u,v) \leq l(u) + l(v)](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)。

**相等子图**：在一组可行顶标下原图的生成子图，包含所有点但只包含满足 𝑤(𝑢,𝑣) =𝑙(𝑢) +𝑙(𝑣)![w(u,v) = l(u) + l(v)](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7) 的边 (𝑢,𝑣)

**定理 1** : 对于某组可行顶标，如果其相等子图存在完美匹配，那么，该匹配就是原二分图的最大权完美匹配。

证明 1.

考虑原二分图任意一组完美匹配 𝑀![M](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)，其边权和为

$$

任意一组可行顶标的相等子图的完美匹配 𝑀′![M'](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7) 的边权和

𝑣𝑎𝑙(𝑀′) =∑(𝑢,𝑣)∈𝑀𝑙(𝑢)+𝑙(𝑣) =∑𝑛𝑖=1𝑙(𝑖)

即任意一组完美匹配的边权和都不会大于 𝑣𝑎𝑙(𝑀′)![val(M')](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7)，那个 𝑀′![M'](data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7) 就是最大权匹配。







**dfs写法 时间复杂度是$O(n^4)$**

```c++
const int N=5e2;
int g[N+1][N+1];
bool vx[N+1],vy[N+1];
int lx[N+1],ly[N+1];
int matchx[N+1],matchy[N+1];
ll d;
    for(int i=1;i<=n;i++){
        for(int j=1;j<=n;j++){
            g[i][j]=-inf;
        }
    }
    for(int i=1;i<=m;i++){
        int u,v,w;
        cin>>u>>v>>w;
        g[u][v]=w;
    }
    fill(lx,lx+n+1,-inf);
    for(int i=1;i<=n;i++){
        for(int j=1;j<=n;j++){
            lx[i]=max(lx[i],g[i][j]);
        }
    }
    for(int i=1;i<=n;i++){
        auto dfs=[&](this auto&&self,int u)->bool{
            vx[u]=true;
            for(int v=1;v<=n;v++){
                if(vy[v]||g[u][v]==-inf)continue;
                if(lx[u]+ly[v]==g[u][v]){
                    vy[v]=true;
                    if(!matchy[v]||self(matchy[v])){
                        matchy[v]=u;
                        matchx[u]=v;
                        return true;
                    }
                }
                else{
                    d=min(d,1ll*lx[u]+ly[v]-g[u][v]);
                }
            }
            return false;
        };
        while(1){
            d=lnf;
            fill(vx,vx+n+1,false);
            fill(vy,vy+n+1,false);
            if(dfs(i))break;
            for(int i=1;i<=n;i++){
                if(vx[i])lx[i]-=d;
                if(vy[i])ly[i]+=d;
            }
        }
    }
    ll an=0;
    for(int i=1;i<=n;i++){
        an+=g[i][matchx[i]];
    }
    cout<<an<<'\n';
    for(int i=1;i<=n;i++)cout<<matchy[i]<<' ';
```



bfs写法 $O(n^3)$

```c++
inline void km(int i){
     fill(vx,vx+n+1,false);
        fill(vy,vy+n+1,false);
        fill(slack,slack+n+1,lnf);
        head=0,tail=0;
        qu[tail++]=i;
        vx[i]=true;
        while(1){
        while(head<tail){
            int u=qu[head++];
            for(int v=1;v<=n;v++){
                if(vy[v]||g[u][v]==-lnf)continue;
                int te=1ll*lx[u]+ly[v]-g[u][v];
                if(te<slack[v]){
                    prer[v]=u;
                    slack[v]=te;
                    if(te==0){
                        vy[v]=true;
                        if(!matchy[v]){
                            int vv=v;
                            while(vv){
                                int uu=prer[vv];
                                int nex=matchx[uu];
                                matchx[uu]=vv;
                                matchy[vv]=uu;
                                vv=nex;
                            }
                            return;
                        }
                        else{
                            qu[tail++]=matchy[v];
                            vx[matchy[v]]=true;
                        }
                    }
                }
            }
        }
        int d=lnf;
        for(int j=1;j<=n;j++){
            if(!vy[j])
            d=min(d,slack[j]);
        }
        for(int j=1;j<=n;j++){
            if(vx[j])lx[j]-=d;
            if(vy[j])ly[j]+=d;
            else slack[j]-=d;
        }
        for(int v=1;v<=n;v++){
            if(vy[v])continue;
            if(slack[v]==0){
                vy[v]=true;
                if(!matchy[v]){
                int vv=v;
                while(vv){
                    int uu=prer[vv];
                    int nex=matchx[uu];
                    matchx[uu]=vv;
                    matchy[vv]=uu;
                    vv=nex;
                }
                return;
                }
                else{
                    qu[tail++]=matchy[v];
                    vx[matchy[v]]=true;
                }
            }
        }
        }
}
```

