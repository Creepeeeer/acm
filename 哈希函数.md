pair哈希

短好写

```c++
struct pairhash{
	size_t operator()(pii&x){
		return (51ull*x.first)^(x.second);	
	}
};
```



长但是不容易被卡

```c++
static uint64_t splitmix64(uint64_t x) {
    x += 0x9e3779b97f4a7c15;
    x = (x ^ (x >> 30)) * 0xbf58476d1ce4e5b9;
    x = (x ^ (x >> 27)) * 0x94d049bb133111eb;
    return x ^ (x >> 31);
}

struct pairhash {
    size_t operator()(const pair<long long,long long>& p) const {
        uint64_t h1 = splitmix64(p.first);
        uint64_t h2 = splitmix64(p.second);
        return h1 ^ (h2 << 1);
    }
};

```

